rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // כל משתמש יכול לגשת רק לנתונים שלו (users/{המזהה שלו}/...)
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
